[pytest]
# reference/ holds read-only cookbooks with their own (paid-API) test suites —
# never collect them.
testpaths = tests
norecursedirs = reference .venv venv reports journal .git
addopts = -q --strict-markers
filterwarnings =
    ignore::DeprecationWarning
