"""The presentation layer: the email is a decision sheet, the research is attached.

Everything here reads typed objects and writes pixels or markup. Nothing here
computes a price, a level or a verdict — if a number appears in the email it was
already computed upstream and frozen into
:mod:`tradingagent.presentation.context`.
"""
